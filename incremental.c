#include <stdio.h>
//pre post increment decrement//
void main()
 {
    int a=20;
	--a; 
    
    a++ || a++;
		// post as more priorities than pre ...and // 
		//its also depends on compiler // 
	printf("%d ",++a); 
	 	
 }
